import { Page, Section, Par, Term, IDL } from '@/components';
import { FigureExampleDLQuery } from '../../utils';

export function Summary() {
  return <Page>
    <Section>
      <Par>This summary will appear soon.</Par>
    </Section>
  </Page>;
}
